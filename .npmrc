auto-install-peers=true
# SEC-T7-15: Block arbitrary postinstall scripts. Native deps (isolated-vm, ssh2, cpu-features,
# esbuild, protobufjs, embedded-postgres) require explicit rebuild via `bun rebuild` in CI/onboarding.
ignore-scripts=true
